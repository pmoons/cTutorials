#include <stdio.h>

int main(void)
{
	int a, b, c, d;
	int e, f, g;
	float h, i, j, k;
	float l;

	printf("a: %d\nb: %d\nc: %d\nd: %d\ne: %d\nf: %d\ng: %d\nh: %f\ni: %f\nj: %f\nk: %f\nl: %f\n", a,b,c,d,e,f,g,h,i,j,k,l);
}

/*The pattern I notice is that variables a, c, and f-k alway appear to be always be 0, but the rest appear to have a variable output, albeit similar on each sucessive execution.*/
